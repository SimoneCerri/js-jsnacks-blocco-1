let enne = Number(prompt("How many number do you want to calculate ?"));
let cubes;

while (enne > 0)
{
    let number = Number(prompt("give me number"));
    cubes = ( number * number * number );
    console.log(cubes);
    enne--;
}
